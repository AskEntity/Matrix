[test]
# web/ tests use happy-dom GlobalRegistrator which pollutes globalThis.
# Run web tests separately: bun test ./web/
# Bun runs all test files in one process (no isolation), so happy-dom's
# global overrides break non-DOM tests. This is Bun's known limitation.
root = "src"
